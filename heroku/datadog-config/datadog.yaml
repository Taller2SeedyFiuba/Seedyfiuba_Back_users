logs_enabled: true
api_key: ${DD_API_KEY}
listeners:
  - name: docker
config_providers:
  - name: docker
    polling: true
    poll_interval: 1s
process_config:
    enabled: 'true'
logs_config:
  container_collect_all: true

hostname: users-service

node_version: 14.16.1
